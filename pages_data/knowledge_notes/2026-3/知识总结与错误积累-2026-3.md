# 知识总结与错误积累-2026-3

## 26-06-09

1. **macOS** 下创建仅系统托盘菜单程序时可以在 `Info.plist` 文件中设置 `LSUIElement=1`（仅后台/代理应用）。这样当程序隐藏在系统托盘菜单（菜单栏）中时，Dock 区域不会显示应用图标。

    `Info.plist` 示例

    ```xml
    <key>LSUIElement</key>
    <true/>
    ```

2. 静态变量如果在进入程序入口 `main()` 前初始化失败，抛出异常且未被捕获的话会引发 `std::terminal` 并直接终止程序。其中具有 **静态存储期** 的全局对象大多会在 `main()` 之前完成 **零初始化**，而动态初始化可能在 `main()` 之前初始化也可能被推迟。函数内的 `static` 变量（局部静态变量，动态初始化）则是在首次执行到该语句时被初始化。对于动态初始化的静态变量而言，如果首次初始化时抛出异常，则该静态变量被视为 **未完成初始化**，后续再次进入该语句时会尝试重新初始化。
3. **Git** 中更新 **子模块 submodule** 可以通过在指定子模块目录下手动进行拉取

    ```sh
    cd path/to/submodule
    git pull
    ```

    也可以使用 `git submodule update --init --recursive` 初始化并拉取所有子模块，后续也可以通过此命令对子模块进行更新。

    如果需要在克隆存储库期间就一并拉取所有子模块，可以通过 `git clone --recurse-submodules GitUrl...` 实现。

4. **Doxygen** 的 `@details` 命令是用于对 `@brief` 命令进行补充说明的，既它是用于更详细的描述对象功能的说明而不是用于描述对象的实现细节。
5. **C++** 中 `std::move` 可以无条件把对象转成右值（准确说是 **xvalue**），表示：你可以搬走它的资源了。而 `std::forward` 是有条件地“保持原本值类别”转发，主要用于模版编程中实现完美转发。

    **详解**

    - `std::move` 用于触发移动语义，让移动构造/移动赋值有机会被调用。本质是类型转换，不做实际移动；真正移动发生在后续构造或赋值里。

        > **注意点**
        > - 被 move 之后的对象仍“有效”，但值通常不再可预期（只保证可析构、可赋值等）。
        > - 不要对 const 对象期待“移动优化”

    - `std::forward` 用于在模板中保留参数传入时的左右值属性。典型场景是转发引用（也叫万能引用）参数 `T&&`。

6. **C++** 中 `const T&` 可以绑定到左值或常量左值，也可以绑定到右值/临时值（如函数返回值）。当其绑定到临时值时，会将临时值的生命周期延长至这个引用本身的生命周期。 `T&` 只能绑定到非常量左值。`T&&` 通常用于绑定右值/临时值（同样会延长生命周期）或在模板编程中用于实现完美转发（万能引用）。

    **常见坑点**

    ```cpp
    const std::string& bad() {
        return std::string("tmp"); // 返回悬空引用，错误
    }
    ```

    这里临时对象在函数结束就销毁了，不会因为返回引用而继续活着。

7. **C++** 中将临时值赋值给 `const T&` 会比赋值给 `T` 少一次拷贝/移动构造的成本。不过现代 C++ 有拷贝消除（尤其是 **C++ 17** 的保证省略场景），很多时候编译器会把中间临时省掉，所以实际生成的产物中未必存在额外的拷贝/移动。
8. **Qt** 中 **QListWidget** 的 `takeItem()` 方法会返回指定位置元素的指针，并从列表中删除这个元素（但不会释放指针指向的内存，后续可以将方法返回得到的元素再插入回列表）。对于需要完全删除列表元素时（释放内存），推荐先通过 `takeItem()` 或其他 `QListWidget` 的成员方法得到需要被删除的元素指针，然后再进行 `delete`。注意，即使你在外部额外维护了一个列表存放元素指针，也不建议直接对其进行 `delete`，因为通过 `QListWidget` 的成员方法取出元素后再进行删除更加符合其功能语义（将元素的所有权转交给你，再安全删除，避免 `QListWidget` 内部可能存在的悬空引用风险）。
9. **Qt** `drag/drop` 事件中，`accept()` 和 `acceptProposedAction()` 方法的区别在于后者不仅表示接受事件，还明确采用系统当前建议的拖放动作。对于具体行为而言，`acceptProposedAction()` 实际上做了两件事：首先把当前实际动作设为 **proposedAction**（例如 Copy/Move/Link 之一），而后调用 `accept()` 标记事件已被处理。
10. **C++** 中 `dynamic_cast` 用于 **运行时安全地向下转换**。

    **适用场景**

    - 基类指针或引用，实际对象类型不确定时。比如有 Base*，实际可能是 A 或 B，你需要只在它真的是 A 时调用 A 的接口。
    - 需要多态层次里做类型分流时。当你无法通过虚函数设计解决，需要根据真实类型走不同分支时。
    - 需要安全失败机制时

    **特点**

    - 指针版失败会返回 `nullptr`
    - 引用版失败会抛出 `std::bad_cast` 异常

    **不建议的场景**

    - 你已经百分百确定类型。可用 `static_cast`（更快，但不做运行时检查）。
    - 可以用虚函数/接口多态解决。优先多态，少做类型判断，设计更干净。
    - 频繁高性能路径。`dynamic_cast` 有运行时开销，热路径里慎用。

    **前提条件**

    - 基类必须是多态类型（至少有一个虚函数函数，通常虚析构）。
    - 只能在有继承关系的类型间使用。

11. **Qt** 中 **QUrl** 的 `isLocalFile()` 方法对 **本地路径** 返回真，并不区分是文件还是文件夹。

## 26-06-12

1. **C/C++** 中 `getc()` 和 `fgetc` 函数在作用与用法上相同。但 `fgetc()` 保证是一个函数，有固定的函数调用开销；而 `getc()` 可以被实现为宏（也可以是函数），宏展开后可能避免函数调用的开销，但可能会多次求值参数。因此：

    ```c
    // 不要对 getc 传入有副作用的表达式
    getc(fp++);  // 危险：若 getc 是宏，fp++ 可能被求值多次
    fgetc(fp++); // 安全：fgetc 保证是函数
    ```

    推荐使用 `fgetc()`，语义更明确，使用也更安全，并且在现代编译器实现中，其与 `getc()` 几乎没有性能差异。

2. **C/C++** 对一个数组名进行强转后，得到的是强转后的指针地址，而不是指向的内存数据。例如

    ```c
    uint8_t buf[8] = {0};
    uint32_t num = (uint32_t) buf; // 错误
    ```

    `buf` 是数组名，会退化为 `char*` 指针，`(uint32_t)` 强转的是指针的地址值，而不是缓冲区里的数据。正确做法是使用 `memcpy()`：

    ```c
    memcpy(&num, buf, sizeof(uint32_t));
    ```

    直接用指针转换再解引用虽然在很多平台能工作，但属于未定义行为（违反严格别名规则）：

    ```c
    uint32_t num = *(uint32_t*) buf;
    ```

3. **C 标准** 明确规定任何对象都可以通过 `char*` 访问（**C11 §6.3.2.3 / §6.5**），这是严格别名规则的特殊豁免。因此对于 `fread()` 及类似函数可以直接：

    ```c
    uint32_t num = 0;
    FILE* file;
    // ...
    fread((char*) &num, sizeof(uint32_t), file);
    ```

    而不需要引入额外的 `char*` 缓冲区等。

4. **C/C++** `fread()` 函数的二，三参数（`size_t size, size_t nitem`），分别是每个元素的字节大小与要读取的元素个数。返回值是成功读取的元素个数（不是字节数）。
5. **C** 语言也可以使用花括号 `{}` 创建 **块作用域**。可以用于限制临时变量的生命周期，避免命名冲突。此外 `switch` 的 `case` 本身不引入作用域，当需要在 `case` 中创建临时变量时，也要使用 `{}` 创建块作用域。
6. 对于多语句宏，需要使用 `do { ... } while(0)` 写法，其主要是避免在 `if-else` 场景下出错。例如

    ```c
    #define MACRO_A(x) { do_something1(); do_something2(); }             // 不推荐
    #define MACRO_B(x) do { do_something1(); do_something2(); } while(0) // 推荐

    // 编译出错：因为 MACRO_A(X) 后的分号截断了 if 语句导致 else 找不到匹配的 if。
    // 如果不添加分号当然可以编译通过，但是语义不完整，不推荐!
    if (condition)
        MACRO_A(x);
    else
        do_something();

    // 正确：即使在宏后面添加分号也能正常工作。
    if (condition)
        MACRO_B(x);
    else
        do_something();
    ```

7. 在局部作用域可以通过 `#define` `#undef` 来临时声明一个宏，并且不会污染其他区域。
8. **C99** **VLA 数组**

    ```c
    int rows = 3, cols = 4;
    int (*arr)[cols] = malloc(rows * sizeof(*arr));

    // 直接用二维下标
    arr[i][j] = value;
    int v = arr[i][j];

    free(arr);
    ```

    对于上面的 VLA 数组 `arr`，其类型是 `int (*)[cols]`（运行时才确定列数的数组指针）。这种类型不能作为函数参数的类型声明（函数签名在编译期确定）。

9. **C** 语言中，任意指针到 `void*` 与 `void*` 到任意指针的转换都可以是隐式的，无需强转。但在 **C++** 中，后者（`void*` 到任意指针）必须进行强转。也就是说

    ```cpp
    void* p = malloc(100);
    int* q = p;                    // 只有 C 中才可行，C++ 中报错。
    int* q = static_cast<int*>(p); // 正确!
    ```

    此外，使用 **C++ 编译器** 时，即使此类代码使用 `extern "C"` 进行声明了，仍然需要进行强转，具体原因见下一条笔记。

10. **C/C++** 中 `extern "C"` 只影响链接符号的名称修饰（name mangling），让 C++ 编译器以 C 的方式处理符号名，使得 C 和 C++ 代码可以互相链接。

    但类型系统和语法规则仍然是 C++ 的，编译器还是用 C++ 规则解析代码。所以 `void*` 不能隐式转换到其他指针类型这条规则依然成立。

## 26-06-14

1. **Windows** `WH_KEYBOARD_LL`（低级键盘钩子）是系统级钩子，不需要 DLL 注入，可以跨位数工作。
2. **Windows** 当目标程序以管理员权限运行时，Windows 安全机制会阻止低权限进程的钩子接收其事件。
3. **C/C++** 标准对基本整数类型的完整规定（**只保证最小宽度，不规定确切大小**）：

    | 类型 | 最小宽度 | 常见实际大小 |
    | ---- | ------- | ----------- |
    | `char` | 8 bit | 8 bit |
    | `short` | 16 bit | 16 bit |
    | `int` | 16 bit | 32 bit（几乎所有现代平台） |
    | `long` | 32 bit | 32 bit（Windows）/ 64 bit（Linux/macOS 64 位） |
    | `long long` | 64 bit | 64 bit |

    **补充**：

    - 标准只规定大小关系：`char` $\le$ `short` $le$ `int` $le$ `long` $le$ `long long`
    - `char` 的符号性（signed/unsigned）是 **未定义行为**，取决于编译器实现。
    - `int` 理论上可以是 16 位（例如在某些嵌入式平台上），因为标准只要求宽度 $\ge$ 16 位。

    **推荐做法（需要明确位宽时）**：

    使用 `stdint.h`（C）/ `cstdint`（C++）中的固定宽度类型。

4. **Doxygen** 中的 `@details` 命令不适用于变量。
5. **Doxygen** 中的 `@brief` 和 `@details` 命令在同一对象上多次出现的话，在生成文档时会被合并成一部分。
6. **C++** 中 `[[deprecated]]` 属性在 **C++14** 中引入，`[[nodiscard]]` 属性在 **C++ 17** 中引入。使用属性的操作读作 **“标记”**，如：“使用了 `[[nodiscard]]` 标记的函数”。
7. **MSVC** 中，调用动态库中的接口时可以不对接口声明标记为 `__declspec(dllimport)`，但是会损失性能。具体原因如下：

    **有 `__declspec(dllimport)` 时**：

    编译器知道该函数在动态库中，直接生成通过 **IAT（导入地址表）** 的间接调用：

    ```plaintext
    call → IAT 条目 → DLL 中的实际函数
    ```

    **无 `__declspec(dllimport)` 时8**：

    编译器不知道函数来自动态库，链接器会为每个导入函数生成一个额外的 **跳转桩（thunk）**：

    ```plaintext
    call → thunk（额外跳转） → IAT 条目 → DLL 中的实际函数
    ```

    相比于有 `__declspec(dllimport)` 的情况多了一次跳转。

8. **C++** 中使用 `constexpr` 声明的函数可以在编译期调用，使用 `constexpr` 声明类的构造函数时，表示构造函数可以在编译期调用，从而允许在编译期创建常量对象。
9. **Python** 中使用 `_` 符号作为变量占位符，例如对于一个循环语句或元组解包而言，可以通过变量占位符忽略某些变量并消除代码检查工具的 `未使用的变量` 警告。
10. **Python** 中 `ord()`（ordinal 序数）与 `chr()` 函数互为逆操作，前者获取单个字符的 Unicode 码点，后者获取 Unicode 码点对应的字符。
11. **Python** 中小整数（通常是 -5 到 256）会被预缓存（存入 **小整数对象池**）用于性能优化与内存节省。
12. **Python** 中 `__name__` 是一个内置的变量，表示当前模块的名称。当一个 Python 文件被直接运行时，`__name__` 的值会被设置为 `__main__`，当文件被导入为模块时 `__name__` 的值是模块的名称（即无后缀的文件名）。故通常使用 `if __name__ == '__main__'` 来判断当前文件是作为主程序运行还是作为模块被使用。
13. 0 的阶乘为 1。
14. **CMake** 可以通过双方括号对值进行包括，其中的值内容不会对特殊符号进行解引用（如变量取值），如下：

    ```cmake
    set(MY_VAR "cmake")
    set(RAW_STR [[Hello ${MY_VAR}]])
    message(${RAW_STR})
    ```

    其中 `RAW_STR` 的值为 `Hello ${MY_VAR}` 而非 `Hello cmake`，双方括号的作用类似于 Python 中的 `r"This is a\n raw string."`。

15. 可以通过下面的方法使 **VSCode CMake** 集成 **vcpkg**，在 **VSCode** 的用户默认设置文件 `settings.json` 中插入：

    ```json
    "cmake.configureSettings": {
        "CMAKE_TOOLCHAIN_FILE": "vcpkg.cmake 完整文件路径", // 例如 to/path/vcpkg/scripts/buildsystems/vcpkg.cmake
        "VCPKG_TARGET_TRIPLET": "平台及架构"                // 例如 x64-windows
    },
    ```

16. [国标中采标与非采标含义](./国标中采标与非采标含义.md)
17. [Windows 中使用 vcpkg 安装 MinGW 库](./Windows%20中使用%20vcpkg%20安装%20MinGW%20库.md)
18. **C++** 中的 `std::unordered_map`，当对其 `insert` 一个已经存在的键时并不会更新对应的值，也不会报错，而是会插入失败并返回一个 `pair<iterator, bool>` 其中 `bool` 值为 `false` 表示未插入元素，而 `iterator` 指向已存在的元素。
19. **C++** 中的 `std::unordered_map`，使用 `[]` 索引取值操作会在键不存在的时候添加新的元素，而键存在的时候更新其值。
20. **Linux** 下可以使用 `xxd <filepath>` 以十六进制形式查看文件内容。
21. **Linux** 下可以通过 `getuid() == 0` 判断程序是否是以管理员权限运行，如果返回真，则当前程序是以管理员权限运行的。
22. **Linux** 和 **macOS** 下可以通过 `export` 命令查看当前系统环境变量，而 **Windows** 可以使用 `set` 命令进行查看。
23. 可以通过以下方式在 **Linux** 或 **Windows** 下添加临时环境变量：

    **Linux** 下添加 `/usr/bin` 至环境变量：

    ```sh
    export PATH=$PATH:/usr/bin
    ```

    **Windows** 下添加 `C:/` 至环境变量：

    ```bat
    set PATH=%PATH%;C:/
    ```

24. **Linux** 下如果需要添加持久化全局的系统环境变量，可以在 `bashrc` 等终端配置文件中添加相应的 `export` 命令，并执行 `source` 命令。
25. **Linux** 配置密码复杂度校验的文件位置：`/etc/pam.d/common-passwd`。
26. **Linux** 下可以通过以下命令使执行 sudo 操作时无需密码验证：

    ```sh
    sudo visudo
    Add: username ALL=(ALL) NOPASSWD:ALL
    ```

27. **Linux** 下可以通过以下命令给指定命令添加别名，示例（为 python3 命令添加别名 python）：

    ```sh
    python='python3' >> ~/.bashrc
    source ~/.bashrc
    ```

28. **Git** 设置网络代理示例：

    ```sh
    git config --global http.proxy http://127.0.0.1:8902
    ```

29. **macOS** 下显示系统隐藏文件命令：

    ```sh
    defaults write com.apple.finder AppleShowAllFiles YES
    killall Finder
    ```

30. **Linux** 和 **macOS** 都属于 **类Unix** 操作系统，均遵循 **POSIX（Portable Operating System Interface）** 标准，POSIX 中的 X 代表 Unix。
Linux 虽然不是 Unix，但实现了 POSIX 标准，确保了兼容性。而是 macOS 基于 BSD（Unix 分支）的，天然符合 POSIX。
31. **CMake** 中可以通过 `set(ENV{ENV_VAR_NAME} VALUE)` 修改环境变量，通过 `$ENV{ENV_VAR_NAME}` 解引用环境变量。
32. `extern "C"` 是 C++ 的语法，告诉 C++ 编译器按 C 的方式进行名称修饰（name mangling）。

    - `.c` 文件由 C 编译器编译，C 编译器本身就不做 name mangling，不认识 `extern "C"` 语法（会报错）
    - `.cpp` 文件由 C++ 编译器编译，需要通过 `extern "C"` 来声明这些符号用 C 的链接约定

    对于有头文件导出声明的场景下。如果在头文件中使用了 `extern "C"` 对 API 进行包装，那么在源文件实现时，只要在定义之前包含了（`#include`）对应的头文件，便不需要在 API 定义处重复添加 `extern "C"`。

    **原因**：C++ 标准规定，如果一个函数定义之前在同一翻译单元中已有 `extern "C"` 的声明，则该定义自动继承 C 链接属性。

## 26-06-17

1. 字节序

    以某 16 位整数类型为例（浮点数或其他多字节数据类型同理），假设其十进制值为 `8`，则对应十六进制为 `0x00` `0x08`，其中 `0x08` 是低位字节，`0x00` 是高位字节。

    - 对于大端序，其在内存中的排列顺序为“**低地址存高字节，高地址存低字节**”，既 `00 08`。
    - 对于小端序，其在内存中的排列顺序为“**低地址存低字节，高地址存高字节**”，既 `08 00`。

    **其他**：

    - 网络字节序使用的是大端序
    - 现代大多数机器使用的都是小端序，因为小端序的“低地址存低字节”在 CPU 运算时永远从最低有效位开始计算，不用等待全部字节，在做类型转换时（例如 `int` 强转 `char*`）低地址直接是数值低位，不用做地址偏移调整。大端序的优势几乎只有人工读内存的时候更“好看".

2. **C++** 中的 `std::numeric_limits<double>::min()` 返回的是 **正的最小非零浮点数**（大约 2.225e-308）（注意别和 `lowest()` 搞混了），`std::numeric_limits<double>` 常见的静态成员有：

    | 函数 | 返回值 |
    | --- | ----- |
    | `min()` | 最小正规范化浮点数（~2.225e-308） |
    | `max()` | 最大规范化浮点数（~1.798e+308） |
    | `lowest()` | 最小规范化浮点数（负）（~-1.798e+308） |
    | `infinity()` | 无穷（正） |
    | `-infinity()` | 无穷（负） |

3. **CMake** Header-only 库通过 `add_library(LIB_NAME INTERFACE)` 命令增加库时，推荐在 `INTERFACE` 后面加上头文件列表，既 `add_library(LIB_NAME INTERFACE YOUR_HEADERS)`。虽然加与不加生成结果都一样，但能够提供”元数据“让某些 IDE（例如 MSVC）正常”看到“这些头文件以显示在项目树中。此外在结合安装导出时也能更规范，尤其是 **CMake 3.23+** 的 `FILE_SET` `HEADERS`，可用于 `install(TARGETS ... FILE_SET HEADERS)`。还能帮助打包/导出时声明”这个库包含了哪些公开头文件“。
4. **C++** 对于一片内存数据，如果要将其重解释为一个指定类型的动态数组 `vector`（假设有一片内存数据 `uchar* data`，要将其重解释为 `std::vector<RGB> rgbs`，其中 `RGB` 数据排布为 `{uchar r; uchar g; uchar b}`），最稳妥的方式不是使用 `reinterpret_cast`，而是使用 `memcpy()`，然后拷贝成 `vector`。对于上面的假设，可以写成以下形式：

    ```cpp
    using uchar = unsigned char;

    struct RGB { uchar r, g, b; };

    static_assert(sizeof(RGB) == 3);

    std::vector<RGB> toRgbVector(const uchar* data, size_t byteSize)
    {
        if (byteSize % 3 != 0)
            throw std::runtime_error("size must be e multiple of 3");

        std::vector<RGB> out(byteSize / 3);
        memcpy(out.data(), data, byteSize);
        return out;
    }
    ```

    如果需要 **零拷贝视图**，那就不使用 `vector<RGB>` 而是使用 `std::span<const RGB>(reinterpret_cast<const RGB*>(data), byteSize / 3);`（`std::span` 于 **C++ 20** 正式加入标准库）。但是使用这个的前提是

    - `data` 必须真的按照 `RGB` 紧密排布
    - `RGB` 必须没有 padding
    - 对齐必须满足 `RGB` 的要求

    所以一般情况下推荐使用 `memcpy()` 生成对应数据类型的 `vector`。

5. **C++** constexpr 函数中定义非 constexpr 变量至少需要 **C++ 14** 标准。
6. 对于模板函数对象，通常可以仅使其成员函数作为模板实现，而对象类型本身可以不必作为模板实现。视情况决定谁使用模板实现，或是均使用模板实现。
7. **C++** 中对一个没有构造函数的结构体/类进行初始化时，必须使用初始化列表进行初始化（例如：`Position pos{x, y}`、`Position{x, y}` 等等（注意此处是花括号而不是圆括号））。

## 26-06-25

1. **Qt** 中的 `QImage::pixel()` 返回一个 `QRgb` 值。对于灰度图而言，其返回值的 `R G B` 值均等于灰度值，或者使用 `qGray(qRgb)` 方法获取灰度值。如果需要更高效地访问也可以使用 `scanLine()` 相关函数直接操作原始数据。如果通过 `setPixel()` 设置灰度图的某个像素，传入的颜色值将会按照亮度公式计算为灰度值。
2. **Qt** 中可以通过 `qRgb()` 构造一个 `QRgb` 类型的值。

## 26-06-29

1. `.sh` 脚本文件中使用 `#` 作为注释符，`.bat` 脚本文件中使用 `::` 或 `REM` 作为注释符。在 `.bat` 中 `::` 比 `REM` 更常用，但在某些特殊场合（例如 `for` 循环内）会报错，此时需改用 `REM`。
2. 在大多数 `.sh` 文件中可以见到 `#!usr/bin/env bash`（**shebang**，也叫 **hashbang**），其作用是告诉操作系统使用哪个解释器来执行这个脚本。具体来说

    - `#!` 是 shebang 标记，内核识别后会调用后面指定的程序来执行该文件。shebang 标记必须写在脚本文件的第一行，而且文件的前两个字符必须是 `#!`。
    - `/usr/bin/env bash` 是通过 `env` 命令在 `PATH` 中查找 `bash` 的位置，而不是硬编码路径（如 `/bin/bash`）

    **为什么用 `env bash` 而不是直接写 `/bin/bash`？**：

    不同系统上 `bash` 的安装路径可能不同（例如 macOS 用 Homebrew 安装的 bash 在 `/usr/local/bin/bash`），用 `env` 查找可以提高脚本的跨平台兼容性。

    **为什么要指定使用 `bash`？**：

    1. 如果直接执行 `.sh` 文件，因为没有 shebang 标记，内核不知道该用什么解释器执行，通常会回退到 `/bin/sh`。
    2. 而 `/bin/sh` 不等同于 bash，在很多系统上它是 `dash`（Debian/Ubuntu）或其他更简单的 shell。
    3. 在这种情况下一旦脚本用了 bash 特有的语法（例如 `[[ ]]`、数组、`source`）就会报错。

    当然也可以显式指定解释器执行脚本（`bash script.sh`），在这种情况下 shebang 无效。

    所以写 shebang 的意义在于可以直接使用 `script.sh` 运行，并保证使用正确的解释器。

    **`bash` 是否自带？**：

    - Linux：几乎所有发行版都预装 bash，但极简系统（如 Alpine）默认只有 `sh`（busybox）。
    - macOS：自带，但从 macOS Catalina（10.15）起默认 shell 改为 `zsh`，bash 仍然存在但版本很旧（3.2），因为 bash 4+ 是 GPL v3 授权，苹果不愿内置。

    如果指定了 bash 作为解释器而找不到 bash，内核会直接报错。

3. **macOS** 是苹果官方的正式写法，**macOS** 实际上是错误拼写。

    苹果在 2016 年将系统名称从 **OS X** 改为 **macOS**，官方文档、发布页面一律使用小写 `mac`、大写 `OS`，与 iOS、watchOS、tvOS 的命名风格保持一致——都是小写平台前缀 + 大写 OS。

## 26-07-01

1. **C/C++** 中 `p[x]` 等同于 `*(p + x)`。如果有数组 `a`，以及指向 `a[2]` 的指针 `p`，那么可以直接通过 `p[2]` 访问 `a[4]`。
2. 在编写循环体时要细心检查终止条件的设定，避免 **差一错误**！！！最好写完后模拟一遍边界情况！！！
3. **C** 语言 “泛型编程” 技巧：

    1. **函数宏展开**：用宏定义整个函数体，调用时 “实例化”：

        ```c
        #define DEFINE_SUM(T)               \
        T sum_##T(T* arr, size_t n)         \
        {                                   \
            T sum = 0;                      \
            for (size_t i = 0; i < n; ++i)  \
                sum += arr[i];              \
            return sum;                     \
        }

        // 编译期 “实例化”
        DEFINE_SUM(int)
        ```

    2. **`.inl` 文件 + 宏 include**：将实现写在 `.inl` 文件中，通过 宏 + `#include` 来 “实例化”：

        ```c
        // sum.inl
        SUM_TYPE sum(SUM_TYPE* arr, size_t n)
        {
            SUM_TYPE sum = 0;
            for (size_t i = 0; i < n; ++i)
                sum += arr[i];
            return sum;
        }
        ```

        ```c
        // sum.h
        #define SUM_TYPE int
        #include "sum.inl"
        #undef SUM_TYPE
        ```

    3. **`void*` 运行时泛型**：类型安全差但无需多份代码：

        ```c
        void sum(
            void* arr, size_t n, size_t elem_size, void* result,
            void (*add)(const void*, const void*, void* result));
        ```

4. 实现二元比较函数时，按照惯例使用严格弱序（`<`），既左操作数严格小于右操作数时返回真，否则返回假：

    1. **C++ STL 标准**——`std::sort`、`std::less`、`std::set` 等都以 `a < b` 作为比较语义（严格弱序）
    2. `klib/ksort` 等知名 **C** 泛型库也用 `lt`（less-than）语义

## 26-07-11

1. **C/C++** 中对空指针解引用是未定义行为，结果取决于平台和编译器，但最常见的是 **运行时崩溃（段错误）**（操作系统的内存保护机制检测到访问地址 `0x00`（受保护页），向进程发送 `SIGSEGV` 信号，程序终止。）

## 26-07-15

1. 三目运算符 `?:` 的两个分支必须是 **表达式** 而不能是语句。也就是说 `i > 0 ? --i : break;` 这类写法是错误的，因为 `break` 是一条语句。
2. 二叉树数据结构常用术语

    | 术语 | 解释 |
    | --- | --- |
    | **根节点（root node）** | 位于二叉树顶层的节点，没有父节点。 |
    | **叶节点（leaf node）** | 没有子节点的节点，其两个指针均指向 `None`。 |
    | **边（edge）** | 连接两个节点的线段，即节点引用（指针）。 |
    | **节点所在的层（level）** | 从顶至底递增，根节点所在层为 1。 |
    | **节点的度（degree）** | 节点的子节点的数量。在二叉树中，度的取值范围是 0、1、2。 |
    | **二叉树的高度（height）** | 从根节点到最远叶节点所经过的边的数量。 |
    | **节点的深度（depth）** | 从根节点到该节点所经过的边的数量。 |
    | **节点的高度（height）** | 从距离该节点最远的叶节点到该节点所经过的边的数量。 |

3. 常见二叉树定义

    | 二叉树 | 定义 | 备注 |
    | --- | --- | --- |
    | **完美二叉树（perfect binary tree）** | 所有层的节点都被完全填满。 | 在完美二叉树中，叶节点的度为 0，其余所有节点的度都为 2；若树的高度为 $h$，则节点总数为 $2^{h+1}-1$，呈现标准的指数级关系，反映了自然界中常见的细胞分裂现象。 |
    | **完全二叉树（complete binary tree）** | 仅允许最底层的节点不完全填满，且最底层的节点必须从左至右依次连续填充。 | 完美二叉树也是一棵完全二叉树。 |
    | **完满二叉树（full binary tree）** | 除了叶节点之外，其余所有节点都有两个子节点。 | - |
    | **平衡二叉树（balanced binary tree）** | 任意节点的左子树和右子树的高度之差的绝对值不超过 1。 | - |
    | **二叉搜索树（binary search tree）** | 任意节点左子树中所有节点的值 $<$ 根节点的值 $<$ 右子树中所有节点的值。 | 不允许存在重复节点。 |
    | **堆（heap）** | 任意节点的值 $\ge$ 其子节点的值（大顶堆）或任意节点的值 $\le$ 其子节点的值（小顶堆） | 堆也是一棵完全二叉树 |

4. **C** 语言中，推荐将指针符号进行右对齐。例如，应该是 `T *var` 而不是 `T* var`。对于类型强转而言应该是 `(T *)p` 而不是 `(T*)p`（注意此处强转的圆括号也不应该与变量名之间保留空格，也就是说不应该是 `(T *) p`）。同样对于声明一个 **unused** 变量也应该使用 `(void)var` 而不是 `(void) var`。
5. 在现代主流 **C** 语言项目中，新代码应该优先使用 `stdbool.h` 中提供的 `bool` 而不是 `int` 实现布尔语义。
6. 在现代主流 **C** 语言项目中，对于具有多个包含了资源释放的返回路径的函数而言，应该使用 **单出口 + `goto cleanup`** 的方法而不是使用多个 **资源释放 + `return`**（便于统一释放资源）。
7. [P 与 NP 问题及其衍生问题答疑（Copilot 对话导出）](./P%20与%20NP%20问题及其衍生问题.md)

## 26-07-31

1. **C++** `std::allocator` 是 c++ 标准容器默认使用的内存分配器。其作用主要有以下两点：

    - 分配/释放原始内存（`allocate/deallocate`）
    - （历史上）负责对象的构造与销毁，现代更多由 `allocator_traits` 统一处理。

    像 `std::vector<T>` 实际上是 `std::vector<T, Allocator>`，第二个模板参数就是 `std::allocator<T>`。

    **为什么需要 allocator 机制？**：

    如果你不想用普通 `new/delete`，比如你想：

    - 用内存池提速
    - 统计分配次数/字节数
    - 固定地址空间（共享内存、arena）
    - 对齐控制

    则可以通过重新实现 allocator 达成目的。（一个可用于标准容器的最小分配器通常要具备 `value_type`、`T* allocate(std::size_t n)`、`void deallocate(T* p, std::size_t n)`）

    **总而言之，allocator 可以把“数据结构逻辑”和“内存策略”进行解耦。**

2. **C++** 模板编程中，对于嵌套类型需要使用 `typename` 关键字进行显示声明。例如：

    ```cpp
    template<class T>
    void f() {
        typename T::value_type x{}; // 没有 typename 会报错
    }
    ```

    `T::value_type` 依赖 T，编译器第一阶段不知道它是“类型”还是“静态成员变量”，所以要使用用 `typename` 明确。

3. **C/C++** 中 `!!(exp)` 宏的作用是把任意表达式的结果标准化至 **0 或 1 的布尔值**。

    - `!expr`：先取逻辑非，结果是 `true/false`
    - `!!expr`：再取一次非，变回“是否为真”

    **效果**：

    - `expr == 0 / nullptr / false` -> `!!expr` 为 `0`
    - 其他非零/非空 -> `!!expr` 为 `1`

    在现代 C++ 中，很多时候可以直接用 `static_cast<bool>(expr)`。

4. **C++** 中 **SFINAE** 约束是 `Substitution Failure Is Not An Error`（替换失败非错误）。意思是：

    编译器在用模板实参去替换模板参数时，如果某个候选模板替换后不合法，这个候选会被“静默丢弃”，而不是立刻报错。然后编译器继续找其他重载。

    “约束”就是利用这个机制，让不满足条件的模板自动失效。

    经典写法就是 `std::enable_if`：

    ```cpp
    template<typename T>
    std::enable_if_t<condition, void> foo(T const&);
    ```

    - 条件为 `true`：有这个函数
    - 条件为 `false`：这个函数签名无效，被丢弃

5. **C++** 中 `std::enable_if` 简化版定义：

    ```cpp
    template<bool B, class T = void>
    struct enable_if {};                 // B=false 时，里面没有 type

    template<class T>
    struct enable_if<true, T> {
        using type = T;                  // B=true 时，才有 type
    };
    ```

    **结论**：

    - 条件为 `true`：`enable_if<...>::type` 存在，等于 `T`
    - 条件为 `false`：`enable_if<...>::type` 不存在，模板替换失败，进入 **SFINAE** 丢弃候选

    也可以使用 `std::enable_if_t<cond, T>` 代替 `std::enable_if<cond, T>::type`。

## 26-08-01

1. **C++** 以下情况：

    ```cpp
    void print(size_t num)
    {
        std::cout << "Num: " << num << std::endl;
    }

    void print(const char* str)
    {
        std::cout << "Str: " << str << std::endl;
    }

    int main()
    {
        print(0); // 报错：产生歧义
    }
    ```

    因为在 **C++** 中，字面量 `0` 可以被标准转换为整数类型，也可以被标准转换为空指针，所以对于上面的代码 `print(0)` 将产生 “有多个 重载函数 "print" 实例与参数列表匹配” 的错误。

    **解决办法**：

    通过 **SFINAE** 对 `print(size_t num)` 进行约束：

    ```cpp
    template<typename Integral,
        typename std::enable_if<std::is_integral<Integral>::value, int>::type = 0>
    void print(Intergral num)
    {
        std::cout << "Num: " << num << std::endl;
    }
    ```

    **原因**：

    模板参数推导产生精确匹配，优先级高于 `0 -> const char*` 的空指针常量转换，歧义自然消失。（优先级：模板参数推导 > 标准转换 > 用户转换）

## 26-08-02

1. **C++** 可以通过 `std::string content((std::istreambuf_iterator<char>(is)), std::istreambuf_iterator<char>());` 快速将流中的数据全部读取到字符串中。
2. **C++** 抛出异常时会进行栈展开（stack unwinding），局部对象的析构函数仍然会被调用，所以无需手动对资源进行释放，这正是 `RAII` 的优势所在。
3. **C++** 判断一个流是否还有内容可以使用以下方法：

    ```cpp
    // 1. 检查 eofbit（只有读到末尾后才置位）
    if (is.eof()) { ... }

    // 2. peek() 是最可靠的——尝试读下一个字符
    if (is.peek() == std::char_traits<char>::eof()) { ... }

    // 3. 流本身转 bool（failbit 或 badbit 置位时为 false）
    if (!is) { ... }
    ```

    推荐用 `peek()`，原因：

    - `eof()` 只在上一次读操作触碰到末尾后才为 `true`，如果还没读过则不会提前告知
    - `peek()` 会主动触发一次读尝试，能准确反映"现在还有没有数据"

## 26-08-10

1. **C++** 使用 `>>` 重载符对输入流内容读取时会在遇到空白字符（`0x00`）时停止，从而导致数据不完整。可以改用 `istreambuf_iterator` 或 `ostringstream`：

    ```cpp
    std::string content((std::istreambuf_iterator<char>(stream)), std::istreambuf_iterator<char>());
    ```

    ```cpp
    std::ostringstream oss;
    oss << stream.rdbuf();
    std::string content = oss.str();
    ```

2. **Doxygen** 中 `@throw` `@throws` `@exception` 三者等价。
3. **C++** `static_assert(sizeof(T) == 0, ...)` 详解

    **核心目的**

    这是一种故意触发编译错误的惯用法，用于在模板被实例化时产生友好的错误信息。

    ---

    **为什么 `sizeof(T) == 0` 永远为 `false`？**

    C++ 标准保证任何完整类型的 `sizeof` 至少为 1（即使是空类也是如此）。因此：

    ```cpp
    sizeof(int)    == 4   // ≥ 1
    sizeof(char)   == 1   // ≥ 1
    sizeof(MyStruct) >= 1 // 保证
    ```

    `sizeof(T) == 0` 在任何合法类型上都不可能为真，所以这个 `static_assert` 一旦被编译器实例化，必然触发编译错误。

    ---

    **为什么不直接写 `static_assert(false, ...)`？**

    ```cpp
    // 错误
    template<typename T>
    struct TagTraits {
        static_assert(false, "unsupported type T");
    };
    ```

    许多编译器（尤其是 MSVC 和旧版 GCC）会在模板定义阶段（不等实例化）就对 `static_assert(false)` 报错，导致即使你从未使用该模板也会编译失败。

    ```cpp
    // 正确：依赖模板参数 T，让断言只在实例化时触发
    template<typename T>
    struct TagGetter {
        static_assert(sizeof(T) == 0, "unsupported type T");
    };
    ```

    通过引用 T，断言变为依赖表达式（dependent expression），编译器必须等到模板被实例化时才求值，从而只有当你真正用了不支持的类型时才报错。
